//Generating a secret no.

i) Means hmra jo standard libary h, rust ki usme yasi koye v functionality ni h, rust ki oo random no. generate kr paye.

- to iska mtlb hame koye 3rd party functionality use krn hoga, 

ii) rust team provide a "rand crate" with said functionality.
- what is crate?
apne jo v source code file bani usme jo v source code h, usko bolte h, crate.


- crate are 2 types.
a) binary                  = jo v kch rust m use hm krte h, sb binary from m h,isko execute kr skte h, directly means hm apne rust k structure ko directly chala skte h.

b) libary crate            = isko hm khud se mtlb independatly khud se run ni kr skte h.
- isko kise program m plugin kr skte h, but directly execute ni.kr skte h.

- example = apne ek libary bani math ki jisme apne ek function dala math ki, add,sub to y libary crate h, usko hm isko kise binary crate k andr use krke run kr skte h.



